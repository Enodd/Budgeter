meta {
  name: Patch Transaction
  type: http
  seq: 5
}

patch {
  url: {{base_url}}/api/transaction/
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "name": "Częściowa aktualizacja",
    "amount": 350.00
  }
}

docs {
  Endpoint do częściowej aktualizacji transakcji.
  Obecnie zwraca tylko 200 OK bez implementacji logiki.
}
