meta {
  name: Delete Transaction
  type: http
  seq: 6
}

delete {
  url: {{base_url}}/api/transaction/?id=1
  body: none
  auth: bearer
}

params:query {
  id: 1
}

auth:bearer {
  token: {{access_token}}
}
